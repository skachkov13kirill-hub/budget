# АУДИТ: БЮДЖЕТ
**Дата:** 2026-04-01
**Бэкап:** tag `pre-audit-2026-04-01` (коммит `8cf87a3`)

## 1. Что это и зачем
Финансовая экосистема для сети из 10 ателье DressCode. Единственный инструмент который используется **ежедневно**. 4 компонента: report.html (сбор данных от филиалов), index.html (дашборд Кирилла), Telegram Bot v9 (на паузе), Google Apps Script v47 (API).

**Масштаб:** 2 764 строк Python + 9 380 строк JavaScript = **12 144 строк кода**

---

## 2. Что работает в продакшне

| Компонент | Статус | Где живёт |
|-----------|--------|-----------|
| report.html (форма отчётов) | Работает | GitHub Pages |
| index.html (дашборд, 5 вкладок) | Работает | GitHub Pages |
| Apps Script v47 (API) | Работает | Google Cloud |
| family.html (семейный бюджет) | Работает | GitHub Pages |
| Service Worker (оффлайн) | Работает | В браузерах |
| Telegram Bot v9 | На паузе | VPS 176.124.208.212 |

---

## 3. Найденные проблемы

### КРИТИЧНО (ломает работу)

**3.1. Г11 — исправлен но НЕ задеплоен**
- Код исправлен в `scripts/unified_apps_script.js` ещё 01.03
- Но этот файл удалён из git (числится как deleted в `git status`)
- Live Apps Script в Google != файлы на диске
- Действие: Открыть script.google.com -> вставить исправленный код -> Deploy -> Manage deployments -> Edit

**3.2. Рассинхрон VPS <-> GitHub <-> Диск**
- `main_v9_updated.py` на VPS != `main.py` на GitHub
- `scripts/unified_apps_script.js` на диске удалён, а `scripts/_clasp_deploy/Код.js` — другая версия
- 3 удалённых файла в git: `_clasp_safe/Код.js`, `unified_apps_script.js`, `Код.js`
- Действие: Решить какая версия каноническая и синхронизировать

**3.3. ПРОЕКТ.md не был закоммичен** -> ИСПРАВЛЕНО (коммит `8cf87a3`)

### ВАЖНО (мешает, но можно жить)

**3.4. Захардкоженные финансовые данные (16+ мест)**

| Файл | Что захардкожено |
|------|-----------------|
| `handlers.py:596` | Дата грейса кредитки "2026-05-05" |
| `financial.py:121,124` | Та же дата + "500 000 руб" лимит + "25.4%" ставка |
| `web/js/business.js:50` | RENT_TOTAL = 419062 |
| `web/js/business.js:56-57` | TAILOR_SHARE = 0.5, PERSONAL_DAILY = 10000 |
| `web/js/payments.js:10-30` | Все 10 RENT_ITEMS + SUBRENT_ITEMS с суммами |
| `web/js/weather.js:7-8,14` | Координаты СПб, средние температуры по месяцам |
| `web/js/family.js:7-80+` | 170+ паттернов категоризации транзакций |

Риск: При изменении аренды/кредитов нужно править код в 3-5 местах. Забытое место = неверные расчёты.

**3.5. Дублирование sheets_api.py и sheets_direct.py**
- Оба модуля делают одно и то же: запись отчётов, чтение филиалов, задачи
- `sheets_api.py` (110 строк) — HTTP через Apps Script — АКТИВНЫЙ
- `sheets_direct.py` (394 строк) — gspread напрямую — НЕ ИСПОЛЬЗУЕТСЯ (не импортирован в handlers.py)

**3.6. Дублирование парсинга дат**
- `handlers.py:141-162` — `_resolve_business_date()`
- `handlers.py:782-810` — `resolve_date()`
- Почти идентичная логика, разные контексты

**3.7. payments.js помечен "Актуально 21.03.2026"**
- Данные об аренде обновляются вручную в коде
- Нет механизма автоматического обновления

### СРЕДНЕ (улучшение)

**3.8. Нет тестов вообще**
- 0 юнит-тестов для Python (financial.py, парсер дат, sheets_api)
- 0 тестов для JavaScript

**3.9. Отсутствие обработки ошибок**
- `sheets_api.py`: все ошибки -> `None` без контекста
- `app.js`: нет offline detection — при ошибке API и пустом кэше рендер падает молча
- `daily.js`: chart rendering crashes если `state.branches.totals` undefined

**3.10. Мёртвый код**
- `sheets_direct.py` — 394 строки неиспользуемого кода
- `handlers.py:83` — устанавливает `dailyChartLoaded` (переменная из daily.js)
- `financial.py:36` — возвращает неиспользуемое третье значение (0)
- `app.js:26-29` — `familyData` определён но не используется
- `overview.js:28` — обращается к `familyData.excludedTransactions` (undefined)

**3.11. Service Worker**
- `sw.js`: CACHE_NAME = `dresscode-v27` — ручное обновление при каждом деплое
- Разные стратегии: report.html = cache-first, app = network-first
- Нет уведомления пользователя об оффлайн-режиме

**3.12. Безопасность (низкий риск)**
- Apps Script URL виден в исходниках (не секрет, но DDoS-точка)
- Нет CSRF-защиты на POST-запросах
- Нет rate limiting

---

## 4. Незакрытые баги из БАГИ_И_УЯЗВИМОСТИ.md

| # | Баг | Статус |
|---|-----|--------|
| В2 | GitHub код устарел vs VPS | Открыт |
| В3 | Два разных Script URL | Не проверено |
| В5 | unified_apps_script на GitHub != Live | Открыт |
| С1 | API-ключи — плейсхолдеры | Без изменений |
| С2 | Сводка 23:30 не проверена | Без изменений |
| С3 | QR-мерчанты не привязаны | Без изменений |

---

## 5. Что нужно доделать (приоритизировано)

### Приоритет 1 — Критично
1. Задеплоить исправленный Apps Script (Г11 починен, но не залит в Google)
2. Синхронизировать код — определить каноническую версию каждого файла
3. ~~Закоммитить ПРОЕКТ.md~~ -> СДЕЛАНО

### Приоритет 2 — Важно
4. Вынести захардкоженные данные в конфиг — аренда, кредиты, даты грейса -> один источник
5. Удалить sheets_direct.py (или пометить deprecated) — мёртвый код запутывает
6. Добавить базовые тесты — минимум для financial.py (5 функций)
7. Обновить payments.js — данные с пометкой "21.03.2026" могут устареть

### Приоритет 3 — Улучшение
8. Унифицировать парсинг дат (2 дублирующие функции)
9. Добавить offline detection в дашборд
10. Автоматизировать CACHE_NAME в sw.js
11. Очистить мёртвый код

---

## 6. Вопросы к Кириллу

1. **Г11 деплой** — Можешь открыть script.google.com и залить исправленный код? Или дать мне доступ через clasp?
2. **VPS синхронизация** — Какая версия бота на VPS считается рабочей? Стоит ли обновить GitHub или наоборот?
3. **Бот вообще нужен?** — Телеграм на паузе. Стоит ли тратить время на синхронизацию бота, или сфокусироваться на веб-части?
4. **Данные аренды** — Аренда 419 062 руб актуальна на апрель? Стоит ли вынести в Apps Script чтобы менять в одном месте?
5. **QR-мерчанты** — 10 merchant_id найдены но не привязаны. Это всё ещё в плане?
6. **Сводка 23:30** — daily_summary() в боте — нужна? Или заменена дашбордом?

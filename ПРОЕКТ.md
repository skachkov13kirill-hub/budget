# БЮДЖЕТ — Финансовая система DressCode

## Что это
Финансовая экосистема для сети из 10 ателье DressCode. Состоит из 4 компонентов:
1. **report.html** — мобильная форма для ежедневных отчётов (ОСНОВНОЙ инструмент сбора данных, заменил бот)
2. **index.html** — PWA-дашборд с аналитикой (используется Кириллом ежедневно)
3. **Telegram Bot v9** — на паузе, Telegram глушат в РФ. Отвечает на запросы (баланс, кредиты, арендa)
4. **Google Apps Script v47** — единое API для всех фронтендов

## Как работает
```
Филиалы → report.html (форма) → Apps Script API → Google Sheets "АТЕЛЬЕ 2026"
                                                           ↓
Кирилл ← index.html (дашборд) ← Apps Script API ←────────┘
```

## Где развёрнуто
- **Dashboard + Forms:** GitHub Pages (skachkov13kirill-hub/-)
- **Telegram Bot:** VPS 176.124.208.212, systemd сервис `bot_v9`, порт — нет (polling)
- **Apps Script:** Google Cloud, deployment v47, URL в config.py (SHEETS_SCRIPT_URL)
- **Данные:** Google Sheets "АТЕЛЬЕ 2026" (ID: 1cRdejpUv8gRyNTQVJPw16pp44FqmX6CZUSdUkWcUa40)

## Структура файлов
```
БЮДЖЕТ/
├── bot_v9/              ← Python Telegram бот (2764 строк)
│   ├── main.py          ← Точка входа, регистрация хендлеров
│   ├── handlers.py      ← Команды + обработка сообщений
│   ├── financial.py     ← Расчёты (баланс, долг, аренда, прогноз)
│   ├── ai_handlers.py   ← Whisper + LLM через Cloudflare Worker
│   ├── sheets_api.py    ← API к Apps Script (HTTP GET/POST)
│   ├── sheets_direct.py ← Прямая запись в Sheets через gspread
│   ├── pdf_parser.py    ← Парсинг банковских выписок
│   ├── utils.py         ← Логирование, утилиты, state
│   ├── config.example.py← Шаблон конфига
│   └── requirements.txt ← Зависимости Python
├── web/                 ← Фронтенд (7459 строк JS)
│   ├── index.html       ← Главный дашборд (5 вкладок)
│   ├── report.html      ← Форма отчётов для филиалов (КЛЮЧЕВОЙ)
│   ├── family.html      ← Семейный бюджет
│   ├── js/app.js        ← Ядро: загрузка данных, кэш, рендеринг
│   ├── js/business.js   ← Карточки филиалов
│   ├── js/daily.js      ← 14-дневный график
│   ├── js/payments.js   ← Отслеживание платежей
│   ├── js/tasks.js      ← Задачи (календарь + дерево + блокнот)
│   ├── js/agents.js     ← Дашборд агентов
│   ├── js/weather.js    ← Прогноз погоды
│   ├── css/app.css      ← Стили (7400+ строк)
│   ├── sw.js            ← Service Worker
│   └── manifest.json    ← PWA манифест
├── scripts/
│   └── _clasp_deploy/Код.js ← Apps Script (30+ endpoints)
├── data/                ← Финансовые таблицы
├── docs/                ← Документация
└── PROMPT_FIX_EXPENSES.md ← 5-фазный план починки Finance Helper
```

## Apps Script API (ключевые endpoints)
| Action | Что делает |
|--------|-----------|
| getAll | Всё разом: филиалы + кредиты + балансы + настройки |
| getBranches | Данные филиалов за месяц/год |
| writeBranchDaily | Записать дневной отчёт (от report.html) |
| getCredits | Список кредитов |
| getBalances | Балансы счетов |
| getPaymentsStatus | Статус оплат (аренда/кредиты) |
| getTasks | Задачи |

## Что работает ✅
- report.html собирает данные от 10 филиалов
- Dashboard показывает план/факт по каждому филиалу
- Вкладки: Бизнес, Daily (14-дневный график), Платежи, Задачи, Агенты
- PWA (оффлайн режим, кэш)
- Семейный бюджет (family.html, отдельный Sheets)

## Что на паузе ⏸
- Telegram бот (не используется ежедневно из-за блокировок)
- Voice → Whisper → Intent (работает, но бот не основной)
- Anthropic API key (невозможно подключить с российского сервера)

## Известные проблемы (аудит 2026-04-01)

### Критично
1. **Г11 — исправлен но НЕ задеплоен** — код починен 01.03, но не залит в Google Apps Script
2. **Рассинхрон VPS ↔ GitHub ↔ Диск** — main_v9_updated.py на VPS != main.py на GitHub; unified_apps_script.js удалён с диска
3. **Захардкоженные данные в 16+ местах** — аренда, кредиты, даты грейса дублируются в handlers.py, financial.py, business.js, payments.js

### Важно
4. **Два пути к Sheets** — sheets_api.py (HTTP, активный) и sheets_direct.py (gspread, не используется) — 394 строки мёртвого кода
5. **Нет тестов** — 0 юнит-тестов для Python и JavaScript
6. **payments.js** — данные помечены "Актуально 21.03.2026", обновляются вручную

### Средне
7. **Мёртвый код** — familyData в app.js, третий return в financial.py, dailyChartLoaded в handlers.py
8. **Service Worker** — ручное обновление CACHE_NAME, нет offline detection
9. **Обработка ошибок** — sheets_api.py возвращает None без контекста, daily.js crashes без totals

## Что дальше (приоритеты)
1. Задеплоить исправленный Apps Script (Г11)
2. Синхронизировать код (VPS ↔ GitHub)
3. Вынести захардкоженные данные в единый конфиг
4. Удалить sheets_direct.py
5. Написать тесты для financial.py

## Аудит
Полный отчёт: `АУДИТ_2026-04-01.md`
Бэкап: git tag `pre-audit-2026-04-01`
